apiVersion: v1
kind: Service
metadata:
  name: graphrag-service
  labels:
    app: graphrag-service
spec:
  type: ClusterIP
  ports:
    - name: health
      port: 8080
      targetPort: health
    - name: metrics
      port: 8081
      targetPort: metrics
  selector:
    app: graphrag-service
