apiVersion: v1
kind: Service
metadata:
  name: email-worker
  labels:
    app: email-worker
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8080
      targetPort: http
  selector:
    app: email-worker
