apiVersion: v1
kind: Service
metadata:
  name: profile-worker
  labels:
    app: profile-worker
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8080
      targetPort: http
  selector:
    app: profile-worker
