apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml
  - configmap.yaml
  - secrets.yaml

labels:
  - pairs:
      app.kubernetes.io/part-of: profile-service
      app.kubernetes.io/managed-by: kustomize

commonAnnotations:
  description: "Queue Service Base Configuration"
