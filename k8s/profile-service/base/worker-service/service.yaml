apiVersion: v1
kind: Service
metadata:
  name: worker-service
  labels:
    app: worker-service
    service: profile
spec:
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
  selector:
    app: worker-service
    service: profile
