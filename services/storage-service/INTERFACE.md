Service Interface Details: Focused description of how this service connects to others. Document the service’s API (endpoints or public functions) and any external APIs or queues it consumes. Essentially, it’s a subset of the global INTERFACE.md but scoped to this service: e.g., “This worker subscribes to jobs_queue from the API” or “Mobile app calls these endpoints on the API, and listens for these push notification events.” Keeping this info in the service folder makes it easy to update when you change the service’s API.
