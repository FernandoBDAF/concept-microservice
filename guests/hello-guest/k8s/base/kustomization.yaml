# hello-guest base — applies on a bare kind cluster, no CRDs required
# (HOST_CONTRACT.md §3). ServiceMonitors live in ../obs.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - web.yaml
  - worker.yaml
  - netpols.yaml
  - ingress.yaml
